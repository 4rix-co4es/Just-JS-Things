        // VARIABLES & CONSTANTS = identifiers

        let f,l ="Am Jir", a = 25;

        console.log(f);
        console.log(l);
        console.log(a);

        const zar = 12.4, n = "aj";

        // LITERAL
        let Goal = "This is literal";
        let reference  = Goal;
        console.log(reference); // This is literal

        const nan = NaN; //Not a number is taken as a space holder


        // SYMBOLS
        const b = symbol();
        const x = symbol("color white");

        b == x //FALSE
        